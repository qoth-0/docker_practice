# docker-compose파일 format에 대한 버전
version: '3.8'
services:
  java-app1:
    build: 
      context: . # 빌드컨텍스트 위치
      dockerfile: DockerfileJava1 # 도커파일명
    image: myjava1:v1 # 이미지명:버전
  java-app2:
    build: 
      context: .
      dockerfile: DockerfileJava2
    image: myjava2:v1

